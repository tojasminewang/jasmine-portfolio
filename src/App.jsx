import Atelier from './variants/Atelier.jsx';

/* ============================================================
   The portfolio uses the Atelier design.
   All content comes from src/data/portfolioData.js — edit that
   file to change what the site says.

   (The four alternate design variants were removed on
   2026-07-13; they live on in git history if ever wanted.)
   ============================================================ */

export default function App() {
  return <Atelier />;
}
